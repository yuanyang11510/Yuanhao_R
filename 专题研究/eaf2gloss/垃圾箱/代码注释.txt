# a：原数据中的所有节点，包括：
# 其他系统创设的元数据节点（interlinear-text-date-created-en、interlinear-text-date-modified-en、interlinear-text-title-en），没有下层节点
# 句子空槽位节点（phrase-txt-zh-CN）
  # 句子标注节点（phrase-txt-ybe、phrase-gls、phrase-segnum），没有下层节点
  # 词空槽位节点（word-txt-zh-CN）
    # 词节点（word-txt），没有下层节点
    # 语素切分节点（morph-txt）
      # 语素标注节点（morph-cf、morph-gls、morph-type、morph-hn）
        
# a - a_ref_0：原数据中的所有子节点和父节点之间的对应关系，包括：
  # 句子空槽位节点 - 句子空槽位节点（右边本来不存在，复制左边的内容，方便后续处理）
  # 句子标注节点 - 句子空槽位节点
  # 词空槽位节点 - 句子空槽位节点
    # 词节点 - 词空槽位节点
    # 语素切分节点 - 词空槽位节点
      # 语素标注节点 - 语素切分节点
  
# a_ref_0 - a_ref_1：所有子节点顺次上移，还剩:
  # 句子空槽位节点 - 句子空槽位节点
  # 句子标注节点 - 句子空槽位节点
  # 词空槽位节点 - 句子空槽位节点
    # 词节点 - 词空槽位节点
    # 语素切分节点 - 词空槽位节点

# a_ref_1 - a_ref_2：所有子节点顺次上移，还剩：
  # 句子空槽位节点 - 句子空槽位节点
  # 句子标注节点 - 句子空槽位节点
  # 词空槽位节点 - 句子空槽位节点

# a_ref_2 - a_ref_3：结果不再变化（句子空槽位节点无法再上移），因此将a_ref_3列删除：
  # 句子空槽位节点 - 句子空槽位节点  
  # 句子标注节点 - 句子空槽位节点
  # 词空槽位节点 - 句子空槽位节点
